# CineTheme Android Release Signing Configuration Example
# Copy this file to 'android/keystore.properties' (or set environment variables in CI)
# DO NOT commit real keystores or passwords to git repositories.

storeFile=../release.keystore
storePassword=YOUR_KEYSTORE_PASSWORD
keyAlias=cinetheme
keyPassword=YOUR_KEY_PASSWORD
